services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: subscree
      POSTGRES_PASSWORD: subscree
      POSTGRES_DB: subscree
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U subscree"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    image: ghcr.io/subscree/subscree-server:latest
    # Not published to the host — reached internally by web as http://server:3001.
    # Add `ports: ["3002:3001"]` if you need to hit the API directly for debugging.
    expose:
      - "3001"
    environment:
      PORT: 3001
      DATABASE_URL: postgresql://subscree:subscree@postgres:5432/subscree
      JWT_SECRET: ${JWT_SECRET}
      ORIGIN_URL: http://localhost:3001
      # Public web URL used in email links (e.g. password reset)
      APP_URL: ${APP_URL:-http://localhost:3001}
      # Email delivery — set RESEND_API_KEY or the SMTP_* vars to enable reminders
      EMAIL_FROM: ${EMAIL_FROM:-Subscree <no-reply@subscree.app>}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
    depends_on:
      postgres:
        condition: service_healthy

  web:
    image: ghcr.io/subscree/subscree-web:latest
    ports:
      - "3001:3001"
    environment:
      API_BASE_URL: http://server:3001
      # Umami analytics (optional) — set both to enable tracking
      UMAMI_SCRIPT_URL: ${UMAMI_SCRIPT_URL:-}
      UMAMI_WEBSITE_ID: ${UMAMI_WEBSITE_ID:-}
      # Google Analytics 4 (optional) — set to enable GA4 alongside Umami
      GA_MEASUREMENT_ID: ${GA_MEASUREMENT_ID:-}
    depends_on:
      - server

volumes:
  postgres_data:
